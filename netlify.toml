[build]
  command = "prisma generate && next build"
  publish = ".next"

[build.environment]
  SECRETS_SCAN_ENABLED = "false"
  SECRETS_SCAN_OMIT_KEYS = "NEXT_PUBLIC_APP_URL,JWT_SECRET"

[functions]
  external_node_modules = ["@node-rs/argon2"]
